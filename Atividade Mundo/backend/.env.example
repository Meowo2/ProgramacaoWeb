# Banco de Dados PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mundo_crud
DB_USER=postgres
DB_PASSWORD=sua_senha_aqui

# JWT
JWT_SECRET=seu_jwt_secret_super_secreto_aqui
JWT_EXPIRES_IN=7d

# Servidor
PORT=3000
NODE_ENV=development

# APIs Externas
OPENWEATHER_API_KEY=sua_chave_openweathermap_aqui
# REST Countries não precisa de chave (gratuita)
